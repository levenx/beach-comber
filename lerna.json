{
  "version": "independent",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": [
        "apps/*",
        "services/*"
      ],
      "message": "chore(release): publish",
      "registry": "https://registry.npmjs.org"
    }
  }
}